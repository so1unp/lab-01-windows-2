#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    char frase[100];

    // Agregar código aquí.
    scanf("%s", frase);

    for (int i = 0; i < frase[i]; i++)
    {

    } // Termina la ejecución del programa.
    exit(EXIT_SUCCESS);
}
